# AI Career Intelligence Platform — Task Breakdown (Table Format)

> Includes 4 new tasks not in your original file: **P1-08** (multi-provider LLM client), **P2-08** (ATS optimization scoring), **P4-06** (SerpAPI + Open Library integration), **P6-10** (Retrieval Agent as a LangGraph node) — all required by your project brief but missing from the original breakdown. No tasks were removed.

| ID | Task | Phase | Priority | Description | Acceptance Criteria |
|---|---|---|---|---|---|
| P0-01 | Create repository structure | 0 – Setup | High | Set up the monorepo skeleton (`backend/`, `frontend/`, `docs/`, `prompts/`, `tests/`, `docker/`) that every later task drops files into. Getting this right early avoids reshuffling once code exists. | - Root has `backend/`, `frontend/`, `docs/`, `prompts/`, `tests/`, `docker/`<br>- `backend/` has `app/routers/`, `app/services/`, `app/models/`, `app/schemas/` pre-created<br>- `prompts/README.md` documents the prompt file naming convention<br>- Root `.gitignore` covers Python, Node, `.env`, `__pycache__`, `node_modules`, vector index files<br>- Root `README.md` has at least a one-paragraph project summary |
| P0-02 | Setup Python environment | 0 – Setup | High | Establish reproducible dependency management using Poetry or pip-tools, pinned to a Python version, so the project installs identically everywhere. | - `pyproject.toml` or `requirements.in/.txt` pins a Python version<br>- Clean-machine install succeeds with no manual steps<br>- Lockfile is committed<br>- Dev deps (pytest, ruff/black) separated from runtime deps<br>- `python -m app.main` imports with no `ModuleNotFoundError` |
| P0-03 | Initialize FastAPI backend | 0 – Setup | High | Scaffold the FastAPI app with routers/services/config separation — the skeleton Phase 1 builds on, kept intentionally thin here. | - `app/main.py` boots via `uvicorn app.main:app --reload`<br>- At least one placeholder router registered via `include_router`<br>- `services/` and `routers/` are separate, no business logic in handlers yet<br>- `/docs` renders Swagger UI showing the placeholder route<br>- App boots without needing DB/Redis running |
| P0-04 | Initialize React frontend | 0 – Setup | High | Bootstrap Vite + React + Tailwind, confirming the toolchain works before real UI is written. | - Vite + React scaffold exists under `frontend/`<br>- Tailwind installed and configured<br>- `npm run dev` serves a Tailwind-styled page (proves it's actually wired)<br>- `npm run build` produces `dist/` with no errors<br>- `.env.example` exists for `VITE_API_BASE_URL` |
| P0-05 | Configure environment variables | 0 – Setup | High | Centralize all config (API keys, DB URLs, feature flags) through `pydantic-settings` so nothing is hardcoded. | - `Settings(BaseSettings)` covers DB, Redis, LLM keys, Adzuna, SerpAPI, feature flags<br>- `.env.example` lists every variable with placeholder values, no real secrets<br>- `.env` gitignored and confirmed absent from `git status`<br>- Settings loaded once via cached factory, not re-parsed<br>- Missing required var causes a clear startup error, not silent `None` |
| P0-06 | Setup Docker Compose | 0 – Setup | High | Containerize Postgres, Redis, backend, frontend so the whole stack starts with one command. | - `docker-compose.yml` defines all four services<br>- `docker compose up` brings all containers healthy, backend reaches Postgres/Redis by service name<br>- Postgres/Redis use named volumes (survive `down` without `-v`)<br>- Backend/frontend mount source for hot-reload in dev<br>- Ports documented, no unintentional collisions |
| P0-07 | Configure Git workflow | 0 – Setup | Medium | Add formatting, linting, and pre-commit hooks for Python and JS to keep code quality consistent. | - `.pre-commit-config.yaml` runs black/ruff and prettier/eslint on commit<br>- A deliberately malformed file triggers hook failure/auto-fix<br>- Lint/format configs are committed, not local-only defaults<br>- `CONTRIBUTING.md` or README documents branch/commit conventions |
| P0-08 | Setup CI pipeline | 0 – Setup | Medium | Add GitHub Actions to run lint and tests automatically on push/PR. | - `.github/workflows/ci.yml` runs on push and PR<br>- Installs backend deps, runs lint + `pytest`<br>- Installs frontend deps, runs `npm run build`<br>- A deliberately broken commit fails the workflow red (confirms it's wired)<br>- Status badge/link added to README |
| P1-01 | Create API structure | 1 – Backend Foundation | High | Flesh out routers/services into the real domain shape: one router per domain (cv, jobs, analysis, interview), delegating to services. | - Separate router files per domain, even if some are stubs<br>- Route handlers contain no business logic — call into `services/*`<br>- Shared `get_db()` dependency used consistently<br>- `/docs` shows routers grouped with sensible tags<br>- Consistent URL conventions (e.g. `/api/v1/...`) |
| P1-02 | Database configuration | 1 – Backend Foundation | High | Wire SQLAlchemy to PostgreSQL with proper session/engine lifecycle. | - Engine built from `Settings` DB URL, not hardcoded<br>- Session dependency yields/closes correctly per request, no leaked connections<br>- Consistent sync-or-async mode (not mixed)<br>- A trivial query succeeds against the Dockerized Postgres<br>- Config supports a separate test DB |
| P1-03 | Create database models | 1 – Backend Foundation | High | Define `User`, `CVProfile`, `JobPosting`, `AnalysisResult`, `InterviewSession` ORM entities with relationships. | - All five entities exist with appropriate column types<br>- FKs/relationships defined both directions where needed<br>- `created_at`/`updated_at` on all entities<br>- Models create matching tables with no errors (create_all or Alembic)<br>- Cascade behavior explicitly decided, not left to default |
| P1-04 | Setup migrations | 1 – Backend Foundation | High | Configure Alembic so schema changes are version-controlled and repeatable. | - `alembic init` committed, `env.py` points at `Settings` DB URL and model metadata<br>- `--autogenerate` matches P1-03 models with no manual edits needed for basic fields<br>- `alembic upgrade head` creates all tables on a fresh Postgres<br>- `alembic downgrade -1` reverses cleanly<br>- Migration files are committed, not gitignored |
| P1-05 | Create API schemas | 1 – Backend Foundation | High | Define Pydantic request/response models mirroring the DB models but decoupled from them. | - `Create`/`Read`/`Update` schema per entity, consistent naming<br>- Response schemas use `from_attributes = True`<br>- No route returns a raw SQLAlchemy model<br>- Nested relationships represented as nested schemas where the frontend needs detail<br>- Schemas validate correctly via `/docs` "Try it out" |
| P1-06 | Add error handling | 1 – Backend Foundation | Medium | Add global exception handlers for consistent, informative JSON errors instead of raw 500s. | - Custom exception hierarchy (`NotFoundError`, `ValidationError`, `ExternalServiceError`) mapped to HTTP codes<br>- Global handler returns consistent `{error, detail}` shape, no leaked stack traces<br>- Missing resource returns clean 404, not 500<br>- Errors logged server-side with debuggable context<br>- At least one deliberate error verified end-to-end |
| P1-07 | Add health endpoint | 1 – Backend Foundation | Medium | Add `/health` reporting DB/Redis status for local checks and deployment. | - `GET /health` returns 200 when DB and Redis reachable<br>- Returns non-200/degraded when a dependency is stopped (verify by stopping Postgres)<br>- Reports per-dependency status, not one boolean<br>- Responds in well under a second |
| **P1-08** | **Configure multi-provider LLM client** *(new)* | 1 – Backend Foundation | High | Your brief commits to Gemini/Groq hosted inference with an Ollama fallback — build a single abstraction every AI feature calls through, avoiding rewrites in Phases 2, 5, 6, 7. | - Single `LLMClient` interface with `.generate()`, provider-independent<br>- Concrete Gemini and Groq implementations, selected via `LLM_PROVIDER` config; Ollama accommodated even if stubbed<br>- Keys read from `Settings`, never hardcoded<br>- Unreachable provider raises `ExternalServiceError`, not unhandled exception<br>- Basic retry/timeout policy in place<br>- Manual test confirms the same prompt returns a response from ≥2 providers |
| P2-01 | Implement CV upload | 2 – CV Analysis | High | Endpoint that accepts a PDF upload, validates it, stores it, and creates the initial `CVProfile` with a processing status. | - `POST /cv/upload` rejects non-PDF with clear 4xx<br>- File size limit enforced with clear error<br>- File stored with a unique, collision-safe filename<br>- `CVProfile` created immediately with `pending`/`processing` status, id returned for polling<br>- Duplicate upload doesn't crash or silently overwrite |
| P2-02 | PDF text extraction | 2 – CV Analysis | High | Extract raw text via PyMuPDF/pdfplumber, handling varying CV layouts reasonably well. | - Works on ≥3 CVs with different layouts (single/two-column, table-based) with readable output<br>- Corrupted/encrypted PDF caught with clear error, no crash<br>- Extracted text stored (raw, pre-parsing) so re-processing doesn't need re-upload<br>- Empty/whitespace-only extraction is checked before proceeding |
| P2-03 | OCR fallback | 2 – CV Analysis | Medium | Fall back to EasyOCR when native extraction yields little/no text (scanned CV). | - Heuristic (low char count) auto-triggers OCR<br>- ≥1 scanned/image-based test CV succeeds via OCR path<br>- OCR output quality spot-checked and documented<br>- OCR path has a timeout / doesn't block indefinitely<br>- CV record notes which extraction path was used |
| P2-04 | Create CV extraction prompt | 2 – CV Analysis | High | Design a structured LLM prompt (in `prompts/`) turning raw CV text into structured JSON matching your `CVProfile` schema. | - Prompt lives in `prompts/`, loaded at runtime not inlined<br>- Instructs strict JSON output matching documented schema, field names match P1-05<br>- Includes ≥1 few-shot example<br>- Tested manually against ≥3 real/sample CVs, outputs reviewed<br>- Explicit instruction for handling missing fields (no hallucination) |
| P2-05 | Implement CV parsing service | 2 – CV Analysis | High | Chain extraction → LLM prompt → structured data → `CVProfile` update, with JSON repair for malformed LLM output. | - Returns validated structured object matching Pydantic schema<br>- Malformed JSON handled via retry/repair, not thrown on first bad response<br>- Success moves status to `completed`, persists structured fields<br>- Repeated failure moves to `failed` with a displayable error, never stuck on `processing`<br>- End-to-end verified on 2–3 sample CVs |
| P2-06 | Add CV validation | 2 – CV Analysis | Medium | Validate LLM-extracted data against Pydantic schemas before persisting. | - Extracted data validated before DB write, errors caught not crashed<br>- Name/skills/experience presence checked; zero-skills CV flagged, not silently accepted<br>- Validation failures update status and log debuggable detail<br>- Test with deliberately malformed mock output confirms validation catches it |
| P2-07 | Create CV test dataset | 2 – CV Analysis | Medium | Assemble sample CVs (varied roles/formats) for testing across Phases 2–3 and the paper's evaluation section. | - ≥5–8 sample CVs: varied formats, seniority, ≥1 scanned/image PDF for OCR<br>- Stored under `tests/fixtures/cvs/` with a short README<br>- No real personal data without consent — synthetic/anonymized only<br>- Referenced by ≥1 automated test |
| **P2-08** | **ATS optimization scoring** *(new)* | 2 – CV Analysis | Medium | Your brief (§8.1) lists ATS optimization check as a core CV feature — missing from the original breakdown. Score common ATS-breaking issues and surface it in analysis output. | - Defined set of ATS checks (5–8), covering formatting/structure not just keywords<br>- Returns a score plus specific issues found, not one opaque number<br>- Runs against existing extracted text/structure, no duplicate re-parsing<br>- Tested against ≥2 ATS-problematic CVs and 1 clean CV, score differentiates them<br>- Result attached to `CVProfile`/`AnalysisResult` for dashboard display |
| P3-01 | Implement embedding service | 3 – Embeddings & Search | High | Wrap a sentence-transformers model (e.g. `bge-small-en-v1.5`) to convert text into vectors, used by CV and job embedding tasks. | - Single `embed()` function, model-agnostic at call site<br>- Model loads once, not reloaded per call (verified by timing)<br>- Embedding dimensionality documented and consistent<br>- Batch embedding works and is meaningfully faster than looping<br>- Sanity test: similar sentences score high similarity, dissimilar score low |
| P3-02 | Generate CV embeddings | 3 – Embeddings & Search | High | Convert structured CV data into embedding vector(s) representing the candidate profile. | - Documented decision on what's embedded (whole-CV vs. per-entry) with tradeoffs noted<br>- Generated automatically once P2-05 parsing completes<br>- Persisted linked to `CVProfile` id<br>- Re-embedding an updated CV replaces, doesn't duplicate |
| P3-03 | Generate job embeddings | 3 – Embeddings & Search | High | Convert job postings into embeddings using the same service as P3-02 for direct comparability. | - Same model/service and dimensionality as CV embeddings<br>- Embedding timing (ingestion vs. lazy) documented<br>- Duplicate postings don't produce duplicate embeddings<br>- ≥10–20 postings embedded and stored for search testing |
| P3-04 | Setup FAISS storage | 3 – Embeddings & Search | High | Persist the FAISS index so embeddings survive restarts and can be updated incrementally. | - Index saved/reloaded on restart, no data loss<br>- Mapping exists between FAISS positions and DB entity ids<br>- Adding a vector doesn't require full rebuild (or cost is measured/accepted)<br>- Index type choice documented with justification<br>- Test: add vector → reload → still searchable |
| P3-05 | Implement similarity search | 3 – Embeddings & Search | High | Given a CV embedding, retrieve top-N similar jobs via cosine similarity / FAISS. | - `search(cv_id, top_k)` returns ranked matches resolved to full `JobPosting` records<br>- Ordering verified correct on a manual sanity check<br>- `top_k` configurable and bounded<br>- Performance measured, well under a second at test scale<br>- Edge case: fewer than `top_k` jobs exist, no crash |
| P3-06 | Add reranking | 3 – Embeddings & Search | Medium | Apply a cross-encoder reranker (e.g. `bge-reranker-base`) to top FAISS candidates. | - Reranker re-scores/reorders top-N (e.g. top 20) results<br>- Reordering compared pre/post rerank with ≥1 documented improvement<br>- Latency measured and acceptable for a live request<br>- Toggleable via config for with/without comparison |
| P4-01 | Create job API client | 4 – Job Market Data | High | Client for Adzuna Jobs API, respecting the free-tier limit (250 req/month) and handling failures gracefully. | - Authenticates with Adzuna via `Settings` keys<br>- Wraps calls in `ExternalServiceError` handling<br>- Basic rate-limit safeguard (counting or caching)<br>- ≥1 real live call verified, response shape documented<br>- Unit-testable with a mocked HTTP response |
| P4-02 | Create job query builder | 4 – Job Market Data | High | Translate a CV profile into an effective Adzuna search query. | - Builder produces query params (keywords, location, category) from structured CV data<br>- ≥3 different profiles produce meaningfully different queries<br>- Sparse CV data degrades to a broader query, doesn't fail<br>- Logic isolated and testable independent of the API call |
| P4-03 | Normalize job responses | 4 – Job Market Data | High | Convert Adzuna's response format into your internal `JobPosting` schema. | - Mapping covers title, description, company, location, salary, url<br>- Missing/optional source fields handled without crashing<br>- Normalized jobs persisted via SQLAlchemy models, not memory-only<br>- Round-trip test (fixture → normalize → assert fields) exists |
| P4-04 | Add job caching | 4 – Job Market Data | Medium | Cache Adzuna results in Redis to reduce redundant calls against the rate limit. | - Repeat queries within TTL return cached results, verified by no increase in API call count<br>- Cache key strategy documented<br>- Sensible TTL/expiry so listings don't go stale forever<br>- Redis-down failure degrades to live call, doesn't break the request |
| P4-05 | Create mock job dataset | 4 – Job Market Data | Medium | Static dataset of representative postings for offline dev/testing. | - ≥30–50 mock postings spanning relevant roles/seniority<br>- Matches normalized `JobPosting` schema exactly (drop-in replacement)<br>- Config flag switches pipeline to mock data<br>- Referenced by ≥1 automated test |
| **P4-06** | **Learning resources API integration (SerpAPI + Open Library)** *(new)* | 4 – Job Market Data | Medium | Your brief lists both APIs (§7) feeding the Learning Path Agent's roadmap — neither had a task, so roadmap generation had no real resource data to draw on. | - SerpAPI client returns course/resource results for a skill query, same error-handling pattern as P4-01<br>- Open Library client returns book suggestions for a topic/skill<br>- Both respect free-tier limits (caching/budgeting, mirroring P4-04)<br>- Both normalized into a common "learning resource" shape (title, url, type, source)<br>- ≥1 live manual test per API verified and documented |
| P5-01 | Create retrieval service | 5 – RAG Pipeline | High | Given a query, retrieve relevant context from FAISS (jobs, market data, resources) to ground LLM generation; underlies the P6-10 Retrieval Agent. | - Accepts a query, embeds it, returns top-k relevant records from FAISS<br>- Works across ≥2 content types via a shared interface<br>- Returned context includes source/id metadata for traceability<br>- Latency measured and acceptable for interactive use |
| P5-02 | Implement RAG pipeline | 5 – RAG Pipeline | High | Connect retrieval and generation into one pipeline: query → retrieve → build prompt → generate. | - Single pipeline function returns a generated answer, handling both steps internally<br>- Retrieved context verified present in the final prompt (logged at least once)<br>- "No relevant context" case handled explicitly, no confident hallucination<br>- End-to-end test with known relevant content produces an answer referencing it |
| P5-03 | Create prompt templates | 5 – RAG Pipeline | High | Reusable RAG prompt templates (in `prompts/`) for skill gap, roadmap, and market trend generation. | - ≥3 distinct templates in `prompts/`, one per generation task<br>- Clear placeholders for context/profile data with documented substitution<br>- Each includes explicit "answer only from context" instruction<br>- Version-controlled text files, not inline strings |
| P5-04 | Implement grounded generation | 5 – RAG Pipeline | High | Ensure LLM answers are actually constrained to retrieved context, not the model's general knowledge. | - Prompt structure separates context from question, instructs grounding<br>- ≥1 test shows correct hedging when no relevant context retrieved<br>- ≥5 test queries spot-checked, claims trace back to retrieved data<br>- Any hallucination found is documented with mitigation attempted |
| P5-05 | Add RAG evaluation | 5 – RAG Pipeline | Medium | Measure retrieval relevance and generation quality on a small eval set. | - Eval set of 10–20 query/expected-context pairs<br>- Retrieval scored with ≥1 metric (precision@k or manual 1–5 rating)<br>- Generation scored with ≥1 method (LLM-as-judge or rubric)<br>- Results written up in `docs/` for reuse in the paper |
| P6-01 | Define LangGraph state | 6 – Multi-Agent System | High | Design the shared state schema all agents read/write — the backbone of the orchestration described in §4.4. | - `TypedDict`/Pydantic model covers all fields every agent needs<br>- Each field's "owner" (writing agent) documented<br>- State is serializable for logging/debugging<br>- Minimal no-op-node graph compiles and runs before adding real agents |
| P6-02 | Implement CV Agent | 6 – Multi-Agent System | High | Wrap Phase 2 CV analysis into a LangGraph node that summarizes the profile for downstream agents. | - Reads `CVProfile`, writes normalized summary to state<br>- Handles sparse/missing CV data without crashing the graph<br>- Node execution logged (input/output)<br>- Unit test asserts expected state keys populated |
| P6-03 | Implement Job Matching Agent | 6 – Multi-Agent System | High | Wrap similarity search + job data into a node that populates ranked matches. | - Reads CV summary, calls P3-05 search, writes ranked matches to state<br>- Uses reranking if enabled via config<br>- Handles zero-matches without breaking downstream agents<br>- Unit test confirms output shape matches Skill Gap Agent's expectations |
| P6-04 | Implement Market Trends Agent | 6 – Multi-Agent System | Medium | Analyze matched jobs to surface in-demand skills/technologies. | - Aggregates skill frequency across matched postings, ranks top skills<br>- Output quantified (e.g. "% of postings mention X") per §8.3<br>- Degrades gracefully with a small job pool (no misleading stats)<br>- Unit test with fixed mock jobs produces expected ranking |
| P6-05 | Implement Skill Gap Agent | 6 – Multi-Agent System | High | Compare candidate skills against market-demanded skills to detect and prioritize gaps. | - Reads CV + market trend data, outputs prioritized missing-skills list<br>- Skills the candidate already has correctly excluded (semantic, not just exact-match)<br>- Output structure matches Learning Path Agent's expectations<br>- Unit test with known fixture produces expected gap list |
| P6-06 | Implement Learning Path Agent | 6 – Multi-Agent System | High | Generate a personalized 30/60/90-day roadmap (§8.4) using skill gaps and learning resources via the RAG pipeline. | - Produces distinct 30/60/90-day sections with specific skills/resources, not filler<br>- Recommended resources trace back to real retrieved data, not invented links<br>- Roadmap differs meaningfully between two different candidates<br>- Unit test confirms all three sections produced from mock gap input |
| P6-07 | Implement Report Agent | 6 – Multi-Agent System | High | Synthesize all agents' outputs into a final structured career report. | - Reads full accumulated state, produces one structured report covering all sections<br>- No redundant re-derivation of facts already computed upstream<br>- Missing upstream data noted in the report, not crashed on or fabricated<br>- Unit test with fully populated mock state produces all expected sections |
| P6-08 | Connect LangGraph workflow | 6 – Multi-Agent System | High | Wire all agent nodes into a single graph with correct ordering, matching the described orchestration. | - Graph compiles with all nodes in sensible order<br>- Full end-to-end run on a real CV executes all nodes, produces a final report unattended<br>- Execution logged step-by-step (node run, state diff)<br>- Node failure handled per a documented policy (halt vs. continue)<br>- Total execution time measured |
| P6-09 | Add agent testing | 6 – Multi-Agent System | High | Test the complete multi-agent pipeline end-to-end beyond individual agent unit tests. | - ≥3 end-to-end runs with different test CVs, each producing a complete report<br>- Structural correctness asserted; content quality spot-checked manually and noted<br>- ≥1 test exercises a failure path (no matches) per the P6-08 policy<br>- Test run times logged as a baseline |
| **P6-10** | **Implement Retrieval Agent (LangGraph node)** *(new)* | 6 – Multi-Agent System | High | Your brief's agent table (§3.1) names a distinct Retrieval Agent — should be a real graph node, not a bare service call, to match the described 8-agent architecture. | - Wraps the retrieval service and RAG pipeline, invoked by other nodes rather than called directly by them<br>- Writes retrieved context to state with clear provenance (query, requesting agent)<br>- Represented as a distinct node in the compiled graph<br>- Unit test confirms correct state population given a mock upstream query |
| P7-01 | Create interview state | 7 – Interview Simulator | High | Define state for an interview session (role, question history, answers, feedback), analogous to P6-01. | - Schema tracks session id, target role, Q&A history, running score/feedback<br>- `InterviewSession` model maps cleanly to/from this state<br>- Supports resuming a session, not only single-shot |
| P7-02 | Generate interview questions | 7 – Interview Simulator | High | Use the LLM client to generate technical questions tailored to the target role. | - Generates relevant, role-specific technical questions, not generic filler<br>- Questions vary across calls for the same role, stay on-topic<br>- Difficulty can be nudged by candidate seniority if available<br>- Generated questions stored as part of history, not ephemeral |
| P7-03 | Evaluate answers | 7 – Interview Simulator | High | Score candidate answers using the LLM with structured, detailed feedback per §8.5. | - Returns a score plus specific written feedback, not just pass/fail<br>- Feedback references the specific question asked<br>- ≥3 test answers (strong/weak/off-topic) produce differentiated scores<br>- Evaluation latency acceptable for a live conversational flow |
| P7-04 | Create WebSocket communication | 7 – Interview Simulator | Medium | Enable real-time chat-style interaction for the interview simulator per §5. | - Endpoint streams question/answer/feedback turns without polling/reload<br>- Disconnect/reconnect doesn't lose session state<br>- ≥1 manual end-to-end multi-turn exchange verified<br>- Mid-session errors communicated over the socket, not a silent hang |
| P7-05 | Store interview history | 7 – Interview Simulator | Medium | Persist completed/in-progress sessions for later review. | - `InterviewSession` records created/updated as the session progresses<br>- Full Q&A + feedback retrievable via API after the session ends<br>- A user can have multiple independently retrievable sessions<br>- Data model supports reverse-chronological listing |
| P8-01 | Create UI structure | 8 – Frontend | High | Set up routing/page structure so subsequent pages have a consistent shell. | - Router defines routes for all major pages, even as stubs<br>- Shared layout/nav exists, no full reload between pages<br>- Basic loading/error/route-guard states handled (e.g. no-CV redirect)<br>- No leftover Vite boilerplate demo content |
| P8-02 | Implement CV upload page | 8 – Frontend | High | Page where users upload a CV and see processing status. | - File picker restricts to PDF, shows backend rejection errors clearly<br>- Polls/listens for status, distinctly showing pending/processing/completed/failed<br>- Routes to dashboard on completion<br>- Shows the specific backend error on failure, not a generic message<br>- Verified with a real test CV against a running backend |
| P8-03 | Create dashboard | 8 – Frontend | High | Main results page: CV summary, ATS score, top matches, links to detail pages. | - Displays real `AnalysisResult` data, not mock content<br>- ATS score and flagged issues visibly surfaced<br>- Top matches listed with title/company/score, linked to source posting<br>- Handles in-progress analysis gracefully (loading state)<br>- Responsive at common breakpoints |
| P8-04 | Create skill gap visualization | 8 – Frontend | High | Visually display missing skills and priority from P6-05. | - Missing skills shown with a visible priority/demand indicator, not plain text<br>- Reflects real backend data across ≥2 different CVs, showing visible differences<br>- Zero-gap edge case handled without broken/empty charts<br>- Priority conveyed with more than color alone |
| P8-05 | Create roadmap page | 8 – Frontend | Medium | Display the 30/60/90-day roadmap with linked resources. | - Clearly separates the three time-horizon sections with skills and resources<br>- Resource links are clickable and open the real source<br>- Reflects real generated roadmap data end-to-end for a test CV<br>- Dense content stays readable (accordions/tabs, not one long list) |
| P8-06 | Create interview interface | 8 – Frontend | Medium | Chat-style UI connected to the WebSocket interview endpoint. | - Sends/receives messages in real time, no manual refresh<br>- Question/answer/feedback visually distinguished<br>- Connection loss surfaced to the user, not a silent freeze<br>- Full test interview (≥2 rounds) completes through the UI<br>- Past interview history viewable |
| P8-07 | Add API integration | 8 – Frontend | High | Ensure all pages consistently call the real backend through a shared client, replacing placeholder data. | - Single API client module used across all pages, no scattered hardcoded fetch calls<br>- Auth/session handling centralized, not duplicated per page<br>- Network errors caught and surfaced consistently<br>- Full manual walkthrough (upload → dashboard → skill gaps → roadmap → interview) works end-to-end with no mock data left |
| P9-01 | Create report template | 9 – Reporting & Export | High | Design the layout/structure of the final report that P9-02 renders into a document. | - Covers CV summary, ATS score, top matches, skill gaps, roadmap, market trends<br>- Defined reusably (template/config), not hardcoded inline<br>- Sections visually distinguished, reviewed against a mock filled example<br>- Styling approach (branded vs. simple/print-focused) decided and noted |
| P9-02 | Implement PDF generation | 9 – Reporting & Export | High | Render the Report Agent's output into a PDF via ReportLab, following P9-01's template. | - Produces a valid PDF that opens without error for a completed `AnalysisResult`<br>- All template sections populated with real data, not placeholders<br>- Long content paginates correctly, no cut-off overflow<br>- Visually reviewed (opened and read) for ≥1 real test case |
| P9-03 | Add download endpoint | 9 – Reporting & Export | Medium | Endpoint to trigger and download the generated report PDF. | - Endpoint streams the PDF with correct `Content-Type`/`Content-Disposition` headers<br>- Incomplete analysis returns a clear error, not a broken/empty file<br>- Reachable via a real frontend button, not only curl/Postman<br>- Access scoped to the report's owner if auth exists |
| P9-04 | Add DOCX export | 9 – Reporting & Export | Low | Optional Word export using python-docx, per §6/advanced features. | - Same report data generates a `.docx` with equivalent section structure<br>- Opens cleanly in Word/LibreOffice, no corruption warnings<br>- Download endpoint supports a format parameter or separate route<br>- Confirmed genuinely deprioritized after MVP criteria are met — doesn't block Phase 10/11 |
| P10-01 | Backend unit tests | 10 – Testing & Evaluation | High | Cover core services (parsing, embedding, search, normalization) with unit tests using P2-07/P4-05 fixtures against an isolated test DB. | - Covers CV parsing, embedding, similarity search, job normalization at minimum<br>- Runs against an isolated test DB via test-specific `Settings` override<br>- No flaky external-API-dependent tests (mocked per P4-01/P4-06)<br>- Suite runs green in CI<br>- Coverage measured, even informally |
| P10-02 | Agent integration tests | 10 – Testing & Evaluation | High | Test the complete AI workflow as a whole, extending P6-09 with RAG and interview flows. | - ≥1 full pipeline test (upload → parse → embed → match → agents → report) asserts final report structure<br>- Interview flow integration-tested end-to-end, at minimum via direct service calls<br>- Uses mock LLM/recorded responses to avoid quota burn and live-model flakiness<br>- Failures produce clear diagnostic output on which stage failed |
| P10-03 | Performance measurements | 10 – Testing & Evaluation | Medium | Measure latency across major pipeline stages for the paper and to spot bottlenecks before the demo. | - Timings captured for parsing, embedding, search, full agent graph run, PDF generation<br>- Measured on a realistic test case with enough detail (mean or sample runs) to be credible<br>- Any notably slow stage flagged with a note on acceptability<br>- Results written up in `docs/` for the paper |
| P10-04 | RAG evaluation | 10 – Testing & Evaluation | Medium | Re-run the P5-05 eval against the fully integrated pipeline to confirm retrieval quality holds up end-to-end. | - P5-05 eval set re-run against real agents/state, not the standalone service<br>- Any regression vs. the P5-05 baseline investigated and noted<br>- Final metrics documented alongside P5-05's for comparison |
| P10-05 | Frontend testing | 10 – Testing & Evaluation | Medium | Test main user flows beyond manual spot-checks. | - Documented manual test scripts for each major flow, confirmed passing before demo<br>- If automated, ≥upload → dashboard flow covered<br>- Common error states (upload failure, no matches, network error) manually verified<br>- Tested against the Dockerized full stack, not just local dev |
| P11-01 | Production Docker setup | 11 – Deployment & Docs | High | Production-oriented Docker config (multi-stage builds, no dev volumes) distinct from dev Compose. | - Multi-stage builds keep images free of dev dependencies<br>- No source volume mounts in the production compose file<br>- Containers run as non-root where feasible<br>- Production compose builds and starts in a clean environment |
| P11-02 | Deploy backend | 11 – Deployment & Docs | High | Deploy FastAPI to your hosting target, connected to a production(-like) database. | - Backend reachable at a public/grader-accessible URL<br>- `/health` reports healthy against real deployed DB/Redis<br>- Secrets configured via platform secret management, not committed<br>- ≥1 real end-to-end request (e.g. upload) succeeds on the deployed backend |
| P11-03 | Deploy frontend | 11 – Deployment & Docs | High | Deploy the built React app pointed at the deployed backend. | - Reachable at a public URL, successfully calls the deployed backend (not localhost)<br>- Production env vars verified via actual network requests in the deployed app<br>- Core flows (upload, dashboard, roadmap) work against the live deployment |
| P11-04 | Configure environment | 11 – Deployment & Docs | High | Finalize production secrets and config across both deployed services. | - All required `Settings` vars set correctly, app starts without config errors<br>- Production LLM provider choice deliberate, not left on a dev default<br>- No dev/debug flags (verbose errors, open CORS) left enabled<br>- Secrets confirmed absent from any committed file or repo history |
| P11-05 | Create README | 11 – Deployment & Docs | High | Write install/usage documentation covering setup, architecture, and how to run the project. | - Covers summary, architecture overview, local setup, Docker Compose instructions<br>- A fresh clone following the README literally results in a working instance (verified, not assumed)<br>- Environment variable requirements documented, referencing `.env.example`<br>- Links to the deployed instance included if applicable |
| P11-06 | Prepare demo scenario | 11 – Deployment & Docs | High | Script and rehearse a concrete end-to-end demo for the defense, avoiding live-demo surprises. | - A specific demo CV pre-tested through the full pipeline end-to-end, confirmed working<br>- Demo script/notes cover each step plus talking points tying back to the architecture (multi-agent orchestration, RAG grounding)<br>- A fallback plan exists for live-demo risk (e.g. LLM API being slow/down) — pre-recorded backup<br>- Full demo rehearsed at least once within the actual defense time constraints |
